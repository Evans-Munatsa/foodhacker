<%= simple_form_for(@comments) do |f| %>

  <div class="form-inputs">
    <%= f.cktext_area :body, id: "body", input_html: { class: 'form-control'}%>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>